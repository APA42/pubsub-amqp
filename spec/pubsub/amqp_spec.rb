require "spec_helper"

RSpec.describe Pubsub::Amqp do
  it "has a version number" do
    expect(Pubsub::Amqp::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
